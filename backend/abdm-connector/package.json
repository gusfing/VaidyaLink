{
  "name": "@vaidyalink/abdm-connector",
  "version": "0.1.0",
  "private": true,
  "description": "ABDM integration Lambda for ABHA ID and consent management",
  "main": "src/index.js",
  "scripts": {
    "test": "jest",
    "lint": "eslint src/ --no-error-on-unmatched-pattern || true",
    "lint:fix": "eslint src/ --fix --no-error-on-unmatched-pattern || true",
    "format": "prettier --write \"src/**/*.js\"",
    "build": "esbuild src/index.js --bundle --platform=node --target=node18 --outfile=dist/index.js"
  },
  "dependencies": {
    "aws-sdk": "^2.1500.0",
    "axios": "^1.6.0",
    "@aws-sdk/client-s3": "^3.450.0",
    "@aws-sdk/client-dynamodb": "^3.450.0",
    "@aws-sdk/lib-dynamodb": "^3.450.0",
    "jsonwebtoken": "^9.0.2",
    "jwks-rsa": "^3.1.0"
  },
  "devDependencies": {
    "@eslint/js": "^9.17.0",
    "esbuild": "^0.19.0",
    "eslint": "^9.17.0",
    "globals": "^15.14.0",
    "jest": "^29.7.0",
    "prettier": "^3.4.2"
  }
}
