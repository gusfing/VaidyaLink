{
  "name": "@vaidyalink/authorizer",
  "version": "0.1.0",
  "private": true,
  "description": "Lambda authorizer for API Gateway JWT validation",
  "main": "index.js",
  "scripts": {
    "build": "esbuild index.js --bundle --platform=node --target=node18 --outfile=dist/index.js"
  },
  "dependencies": {
    "jsonwebtoken": "^9.0.2",
    "jwks-rsa": "^3.1.0"
  },
  "devDependencies": {
    "esbuild": "^0.19.0"
  }
}
